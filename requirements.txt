# Core
flask>=3.0
python-dotenv>=1.0
requests>=2.31

# PDF parsing
pymupdf>=1.24

# LLM (direct Ollama HTTP API — no LangChain wrapper needed)

# Headless browser / scraping
scrapling>=0.2
curl_cffi>=0.7
playwright>=1.58.0
msgspec>=0.21.1
browserforge>=1.2.4

# Database (stdlib sqlite3 is used directly — no extra package needed)
